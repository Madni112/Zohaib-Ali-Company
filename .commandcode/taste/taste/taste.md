# Taste
- Postgres/Supabase schema fact for this project: several identifiers are mixed-case (camelCase) — e.g., the column `opening_stocks."itemName"` (and related `NEW."itemName"`/`OLD."itemName"` in triggers) — so any SQL migration/function the agent writes must quote such names (`os."itemName"`), because an unquoted `os.itemname` gets folded to lowercase and fails at runtime with `42703: column ... does not exist`. Double-check every reference to a known camelCase column before handing the SQL over. Confidence: 0.7
- Before applying risky or hard-to-reverse changes to live/shared data (e.g., database schema or stored records), wants a guaranteed way back: reversible migrations (separate apply/undo SQL scripts), a safety backup, and a fallback so a bad change can never zero out displayed data; he also explicitly prefers being told the exact steps first so he can run a manual backup himself ("tell me first i will backup manually (better)") — wait for his go-ahead after he has backed up rather than charging ahead. Even after a plan has already been explained, he asks to re-hear it immediately before work starts ("before proceed (again tell me what we gonna do)") — restate the full step-by-step plan as a final checkpoint and do not begin until he confirms. Confidence: 0.95
- In this Supabase project, when he says "back up" he means versioning the schema/logic (the migration SQL files that create tables, triggers, functions — treated as the source of truth and kept reversible), NOT exporting or duplicating the data rows; he explicitly corrected "backing up data" to "backing up supabase tables and logics". Confidence: 0.7
- Prefers list/table rows to stay compact and summarized (e.g., "3 Items" count, or a single "4 DCs" chip in the invoice list's DC No. column) rather than rendering every sub-record inline (stacking multiple chips/cards inside one cell is treated as a defect); details should be reachable by clicking the summary value, which opens a popup/modal showing the full per-item breakdown with all details ("why not simply make total counting of items e.g. 3 ... and make it clickable ... will open a popup" / "why don't we show something clickable which will open the popup and in that popup we can see all dc"). Confidence: 0.8
- Prefers review/analysis requests to be read-only: when asked to review/analyze code or asked to "just tell"/explain a reported issue (e.g. "(just tell)"), do not make edits or write files — report/explain findings first and end by offering to implement the fix, waiting for an explicit "yes"/"fix it" before touching code. Confidence: 0.97
- Iterates by implementing fixes himself between review rounds and then asking the agent to re-check ("check again now, I've made some changes") — re-run the analysis against the current working tree/diffs rather than restating the earlier report. Confidence: 0.8
- Prefers the simplest possible data model over building out generic status machinery: when a flow only ever uses one status value (e.g. transfers always saved as "Confirm"), he'd rather drop status filtering/checks entirely and treat every row as the completed movement, rather than add more statuses or generic handling. He intentionally removed draft logic and does not want it proposed/re-added — even when a status-filter bug could tempt draft/pending-status handling, he instructs "don't want draft logic, fix it your way without adding draft logic" (every saved record just counts as the real movement). Confidence: 0.95
- When analyzing why a value/screen is wrong, trace and explain where the data actually comes from (source tables, formulas, call chain) — the user explicitly wants to know "where it's getting from", not just a list of bugs. Confidence: 0.8
- When he asks for a quick check / simple verdict ("is it working fine or not", "tell me in simple short answer") or asks to "explain in short / in easy words", reply in short plain-language — layperson terms, minimal jargon, simple structure — not another long detailed technical report. Confidence: 0.95
- Has made brevity and simplicity a standing default ("from now keep replying quickly and reply me in simple short answer") — keep replies short and fast as the ongoing baseline, not only when a quick verdict is explicitly requested. Confidence: 0.9
- Wants analogous screens to reuse the same interaction pattern and visual chrome already built elsewhere in the app — e.g., asked to make the Opening Stock Add form handle multiple products "like stock transfer does", the Sale Invoice product column "same as opening stock", and the invoice's add-line button to match the stock transfer page ("fix + add row line button ui same as stock transfer add have"), expecting the established line-item style/buttons to be mirrored rather than a new structure or one-off styling invented. This consistency expectation extends beyond visuals to behaviour/logic too — when he asked whether another screen's "ui or logic should be same as delivery challan's", he wanted analogous screens to share the same status flow and stock rules rather than duplicating them separately. His domain model is that SDQ is the shop-side and DC the warehouse-side of ONE flow ("sdq is for shop and dc is for warehouse and their logic is same"), and he pushes back ("huh? both are different?") when told they differ — so confirm/reuse a single shared status flow and stock rules across both and frame them as two views of the same process, treating only the extras (DC's vehicle/driver/freight/gate-pass, SDQ's queue/approval) as the differences. He treats the Delivery Challan page as the concrete gold standard to copy onto other sales screens ("fix sdq same as delivery challan", "you compare both and see where sdq should have to look like dc") while insisting the DC page itself must not be touched ("no delivery page is fine for me (ui wise ux wise)") — so mirror DC's style onto the other page instead of changing DC. Confidence: 0.9
- For multi-entry forms, prefers a shared header for fields common to all rows — including batch number (he explicitly prefers header-level batch over per-row: "header batch would be good (and user friendly)") — with only product-specific fields (product, qty, cost, expiry) in the per-row line items table; also prefers leaving existing single-record flows (e.g., edit mode) untouched when adding multi-row entry rather than expanding scope. Confidence: 0.85
- Wants data-integrity guardrails in multi-row entry forms: the same product must not be enterable/selectable in more than one row of the same document — duplicates should be flagged live (red border + inline message under each offending row) and the save/submit itself blocked with a descriptive toast that names the product and row numbers. He confirmed wanting this guard on the stock transfer page with a direct "yes" and accepted the finished behavior without further changes. Confidence: 0.85
- Delegates writing the wording of user-facing validation/error messages to the agent ("give text with your own logic on that line") rather than dictating exact copy. Confidence: 0.6
- Searchable/autocomplete dropdowns in entry forms should open immediately on focus/click showing all available options (so the user can browse and interact before typing), not require a typed query to make the dropdown appear; typing then narrows the list. They must also stay open/reliable while the user is interacting — a dropdown that flashes open and disappears on its own within a second (reported in the Code/SKU and product-designation inputs on the stock transfer page) is a bug to hunt down (e.g., setTimeout/blur/state race closing it) and fix. Confidence: 0.75
- When a product autocomplete list is shown before any typing, order it by business relevance — highest current stock first (ties broken alphabetically), capped at a fixed count like 60 — so users can quickly pick what's actually available; a typed query still switches to normal name/code filtering. Confidence: 0.5
- When proposing a feature/UX idea — or checking an implementation approach he has just made ("now adding a space in search also making load (do you think its good)") — the user explicitly asks for the agent's honest assessment first ("what you think (is it good?)" / "what do you think about it"): give a quick genuine opinion with brief reasoning (e.g., flagging a wasteful every-keystroke DB load and recommending debouncing/trimming) before continuing, rather than silently diving in. Confidence: 0.8
- Wants product search/autocomplete matching to be robust to messy stored data — e.g. a name entered with a double space ("Porta  Access MT Cp Bath Set") must still match when the user types it with normal single spaces, so matching should collapse/normalize whitespace before comparing, and this normalization should be applied consistently to every analogous picker page (he hit the issue on the Stock Transfer picker and the same fix was expected on the Opening Stock picker). Confidence: 0.55
- Prefers data to be normalized/cleaned at the point of entry (write time), not just tolerated at read time: he asked to "collapse extra space while adding to for better ux", so free-text fields like product names should have extra whitespace collapsed and be trimmed on save across every entry point (both the Add Product form and bulk upload), so messy values never enter the DB in the first place. Confidence: 0.6
- Keyboard navigation in dropdown/autocomplete lists must be fully usable: pressing arrow keys should move the highlight AND auto-scroll the highlighted option into view (scroll-into-view), so the active item never stays hidden off-screen while the user holds the arrows. Confidence: 0.55
- Adding a new line/row to a multi-row entry form should auto-focus the new row's first input (and open its dropdown where applicable) so the user can keep typing immediately without an extra click ("clicking add product line should aut focus on new row's product input"). Confidence: 0.45
- Treats a multi-line submission as ONE document end-to-end, not a set of independent rows: all lines of a single submission must share one document-level identifier (a single shared Stock No per batch), and the corresponding list page must show a single grouped entry per document — one row per Stock No, with a Qty column totaling all lines — rather than one row per line item. He later refined the row layout: once a batch can hold many items, do not inline every line in the table ("what if I created with many items… why not simply make total counting of items e.g. 3"); instead show a compact clickable item-count pill that opens a popup/modal listing each product with its qty and full details. Confidence: 0.8
- Very performance- and scalability-conscious for every user-facing surface: repeatedly asks whether pages (stock checks, product page, invoice page) will stay fast once there are thousands of rows, and pushes past one-off trade-offs (like "slow once, then instant") toward solutions that keep ALL interactions and page loads light as data grows — e.g., precomputed summary tables read instead of raw ledgers. Expect him to probe "is it possible?" and to want the same speedup applied app-wide, not just to the page under discussion. Confidence: 0.6
- For list pages, wants a user-changeable page-size selector (10, 20, 30… entries) and endorses server-side pagination — loading only the current page's rows from the DB with search/sort done in the DB — rather than loading all rows and paginating in the browser. He gives a conditional green light to implement it when it is genuinely the faster/better approach ("do it if it is the faster good way"). Confidence: 0.7
- Hands over large/tabular outputs (e.g., requested SQL query results / DB schema dumps) either as CSV files saved on disk — kept under Downloads\csv in a subfolder per project ("Zohaib Ali"), replying with nothing more than a bare folder path for the agent to explore and read — or, when he is told to paste the output/rows, as the raw result set dumped inline into chat (JSON text, no commentary). The delivery mechanism follows whatever the agent explicitly asks for; whichever form he picks it is a raw, uncommented artifact the agent must interpret. Large inline pastes can arrive truncated mid-table (a full schema dump got cut off at `sales_invoices`, silently dropping later tables), so when completeness matters ask for a narrow filtered query (only the missing tables/columns) or a CSV export, and verify the received output is complete before building on it. Confidence: 0.6
- On DB-backed list pages, endorses debounced+trimmed search so typing never fires a DB load per keystroke: when the agent flagged that a plain keystroke-to-load search is wasteful and proposed a ~400ms debounce plus trimming so a lone space (whitespace-only input) triggers nothing, he agreed ("yes") and accepted the implementation unchanged. Apply debounce/trim to search inputs that hit the server rather than letting raw keystrokes reload. Confidence: 0.6
- Non-technical user: for any task he must do on his own machine, give explicit, click-by-click, beginner-level instructions with exact file paths and Windows UI steps (e.g., "enable View → Hidden items to see .env", exact paths like `C:\Users\user\...\project\.env`) — he cannot reliably use complex developer workflows (console debugging, localStorage, searching minified sources) to find app config, and will ask "where can I find .env?" rather than locate it himself; when he tries such a step he reports the output as "confusing" and abandons it. When one approach stalls, pivot to an even simpler numbered click-path and explicitly tell him to ignore the confusing earlier step — this works: given the exact "Network tab → filter by `supabase` → read Request URL" steps he succeeded and pasted back the supabase URL (`wpzwntbgpeiiclytuuht.supabase.co`). He tends to reply with the raw artifact he was asked to produce (a URL, bare folder path, CSV path, or even an API/publishable key like `sb_publishable_…`) with no commentary, expecting the agent to interpret it and drive the next step; when he hands over a credential/key, don't treat it as proof of schema — recognize it identifies the project but can't substitute for an actual data/schema dump. Prefer paths/actions the agent can perform or verify directly over pushing discovery onto him. Confidence: 0.9
- Runs SQL literally by pasting it into the Supabase SQL editor and reports the raw Postgres error back (e.g. `ERROR: 42601: syntax error at or near "if"` from a stray parenthetical note left inside the script), so every script handed to him must be a complete, syntactically valid, copy-paste-and-run artifact: no pseudo-code, keep-in-mind parentheticals, placeholder tokens or commented-out sub-steps mixed into the runnable block — put warnings/review notes as pure `--` comment lines, never inside a statement. Note that prose outside the code block does NOT protect him: he pastes the whole message, explanation included — a guidance sentence like `If it says relation "public.sales_returns" does not exist, then ...` sitting next to the SQL got copied into the editor and produced `ERROR: 42601: syntax error at or near "If"`. So isolate the runnable statements in their own code block with no adjacent explanatory prose, and whenever you tell him what to run, say explicitly "run only these N lines (nothing else)". He likewise copies the markdown code fences themselves: pasting a fenced snippet produced `ERROR: 42601: syntax error at end of input` from the stray ``` lines, so for short fixes give each statement singly and tell him to copy only the line itself with no backticks. Sanity-check the whole script compiles before sending it. He runs it against whatever DB state actually exists, so a script must also not assume every expected object is present: he hit `ERROR: 42P01: relation "public.logistics_transportation" does not exist` when a loop referenced tables absent from that project (the fix wraps each table in `IF to_regclass('public.' || t) IS NOT NULL THEN`), so guard per-object DDL/policy/trigger operations against missing relations rather than letting one missing table abort the whole run. Confidence: 0.8
- When a popup/detail list can mix records from several locations/warehouses, wants them divided location-wise with a tab-switch control styled exactly like the one already used on the Reports page (a rounded segmented pill strip with the active tab highlighted green, one tab per location with counts) — reusing the existing Reports-page tab styling, not an invented one. He rejected a plain custom pill row ("make switch good not that way"), then also rejected a dropdown/select ("noo bro not in that way too i need switch tabs (like i have in reports)"), and wants NO aggregate "ALL" option ("don't want all tab"); the popup should open on the first location showing only that location's records, hiding the switch entirely when there is only one location. Confidence: 0.85
- When two screens should be aligned, wants the agent to do the comparison itself instead of being asked to point out what's wrong: told "you compare both and see where sdq should have to look like dc" after the agent asked him to name the specific part that felt off. Read both files, diff the layouts/behaviour yourself, and present a concrete ranked list of the real differences to fix, rather than pushing the identification work back onto him. Confidence: 0.7
- In entry-form line-item tables, prefers a single consolidated product column over separate Code and Product columns: one searchable field per row (name or code) with the picked code displayed beneath it, matching the established Opening Stock layout ("only for product getting: there is only 1 column"). Don't add a dedicated SKU/Code column when the same picker already shows the code. Confidence: 0.55
- When aligning one screen to another, "align the UI" means the WHOLE page, not just the piece the agent happened to diff: after the agent matched only the page header on SDQ, he pushed back with "not only header the ui too", so also port the display conventions of the reference page everywhere they appear — quantity formatting (DC's tile-aware "X Boxes + Y Pcs" instead of plain numbers), summary/status badge chips, per-row chips, status labels, and the popup/detail tables — so the two pages look and read identically. Don't stop at the most obvious element; sweep the whole page for remaining differences. Confidence: 0.8
- Wants exactly one control per action on a screen — no duplicate/redundant buttons: he rejected the SDQ action bar for having two print buttons (a plain "Print" plus "Print Gate Pass"), wanting only the single canonical button. When mirroring a reference page, diff the action bars too and delete overlapping/superseded controls instead of leaving the old one in place. Confidence: 0.5
